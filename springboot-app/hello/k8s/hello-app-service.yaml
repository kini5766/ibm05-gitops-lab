apiVersion: v1
kind: Service
metadata:
  name: hello-app-svc
  namespace: hello-app
spec:
  # pod와 클라이언트를 연결하기 위해 서비스가 NodePort를 이용해 url로 외부와 연결
  type: NodePort
  selector:
    # 서비스의 label와 일치
    app: hello-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
